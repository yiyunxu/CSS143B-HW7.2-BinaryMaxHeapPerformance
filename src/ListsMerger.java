import java.util.List;

public class ListsMerger {
    // merge the give a k descendingly sorted linked lists into one linked list
    // to be implemented using MaxPriorityQueue in O(Nlogk) time complexity where
    // N is the number of nodes in the final list
    public List<Integer> merge(ListNode[] lists) {
        // homework
        return null;
    }
}
